---
import Tags from './Tags.astro';

interface Props {
	title: string;
	description: string;
	tags: string[];
	projectUrl?: string;
	sourceCodeUrl?: string;
	imageUrl?: string;
}

const { title, description, projectUrl, sourceCodeUrl, tags, imageUrl } = Astro.props;
---

<div class="project-card">
	{imageUrl && (
		<a href={projectUrl} target="_blank" rel="noopener noreferrer" class="project-image">
			<img src={imageUrl} alt={title} />
		</a>
	)}
	<div class="project-content">
		<h2>{title}</h2>
		<p class="description">
			{description}
		</p>
		<Tags tags={tags} />
		<div class="links">
			<a
				href={projectUrl}
				target="_blank"
				rel="noopener noreferrer"
				class="btn-link btn-link-external small">Check it out</a
			>
			<a
				href={sourceCodeUrl}
				target="_blank"
				rel="noopener noreferrer"
				class="btn-link btn-link-external small">Source Code</a
			>
		</div>
	</div>
</div>

<style>
	.project-card {
		border-radius: 0.5rem;
		overflow: hidden;
		box-shadow: var(--shadow-card);
		display: flex;
		flex-direction: column;
		height: 100%;
	}

	@media (prefers-color-scheme: dark) {
		.project-card {
			border: 1px solid var(--color-border);
		}
	}


	.project-image {
		display: block;
		width: 100%;
		height: 200px;
		overflow: hidden;
		text-decoration: none;
	}

	.project-image img {
		width: 100%;
		height: 100%;
		object-fit: cover;
	}


	.project-content {
		padding: 1.5rem;
		display: flex;
		flex-direction: column;
		flex-grow: 1;
	}

	.project-card h2 {
		font-size: 1.5rem;
		margin-bottom: 0.75rem;
		color: var(--color-text);
	}

	.description {
		color: var(--color-text-light);
		margin-bottom: 1rem;
		line-height: 1.5;
	}

	.links {
		display: flex;
		gap: 1rem;
		flex-wrap: wrap;
		margin-top: auto;
	}
</style>
