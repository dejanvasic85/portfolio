---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<section class="cv-section">
	<h2>{title}</h2>
	<slot />
</section>

<style>
	.cv-section {
		margin-bottom: var(--space-2xl);
	}

	h2 {
		font-size: 1.8rem;
		margin-bottom: 1.5rem;
	}
</style>
